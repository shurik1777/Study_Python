# Задача 2: Найдите сумму цифр трехзначного числа.

# *Пример:*

# 123 -> 6 (1 + 2 + 3)
# 100 -> 1 (1 + 0 + 0) |

numbers = input('Введите цифры: ')

print(numbers)

sum = 0
for char in numbers:
    sum += int(char)

print(f"Сумма цифр: {sum}")
